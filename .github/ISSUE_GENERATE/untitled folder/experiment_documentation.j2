name: Experiment Documentation
description: Submit metadata for a CMIP7 experiment configuration.
title: "[EMD] Experiment Documentation"
labels: ["emd-submission", "experiment"]
body:
  - type: markdown
    attributes:
      label: Instructions
      description: |
        Please fill out the following fields to document an **Experiment Configuration**.
      value: |
        Please fill out the following fields to document an **Experiment Configuration**.

  - type: dropdown
    id: experiment_id
    attributes:
      label: Experiment ID
      description: Select the CMIP7 experiment identifier
      options:
{% for key in experiments.keys() %}
        - "{{ key }}"
{% endfor %}
    validations:
      required: true

  - type: dropdown
    id: activity
    attributes:
      label: Activity/MIP
      description: Select the primary MIP activity
      options:
{% for key in activities.keys() %}
        - "{{ key }}"
{% endfor %}
    validations:
      required: true

  - type: dropdown
    id: tier
    attributes:
      label: Experiment Tier
      description: CMIP7 tier classification
      options:
        - "0"
        - "1"
        - "2"
        - "3"
    validations:
      required: true

  - type: checkboxes
    id: required_model_realms
    attributes:
      label: Required Model Realms
      description: Select the model components/realms REQUIRED
      options:
{% for key in source_types.keys() %}
        - label: "{{ key }}"
{% endfor %}

  - type: input
    id: reference_doi
    attributes:
      label: Primary Reference DOI
      description: DOI for the primary reference
      placeholder: "https://doi.org/..."
    validations:
      required: true
