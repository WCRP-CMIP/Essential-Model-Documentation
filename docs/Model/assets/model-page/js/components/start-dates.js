// components/start-dates.js — SUPERSEDED (safe to delete).
//
// The "Simulated start dates" cumulative chart is now a subsection of the
// combined "ESGF Publication Data" box. See components/esgf-publication.js,
// which uses cumulativeChartNode() from components/cumulative-chart.js keyed on
// `start_datetime`.
